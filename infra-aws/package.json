{
  "name": "vantaum-infra-aws",
  "version": "0.0.1",
  "private": true,
  "description": "AWS CDK stack for VantaUM. Parallel deployment target — application code lives at the repo root.",
  "bin": {
    "vantaum-infra": "bin/vantaum.js"
  },
  "scripts": {
    "build": "tsc",
    "watch": "tsc -w",
    "cdk": "cdk",
    "synth": "cdk synth",
    "diff": "cdk diff",
    "deploy": "cdk deploy"
  },
  "devDependencies": {
    "@types/node": "^22.10.2",
    "aws-cdk": "^2.171.1",
    "ts-node": "^10.9.2",
    "typescript": "^5.7.2"
  },
  "dependencies": {
    "aws-cdk-lib": "^2.171.1",
    "constructs": "^10.4.2"
  }
}
